---

---

<button>Login</button>

<style>
  button {
    background-color: var(--color-on-ui-primary);
    width: 100px;
    justify-self: end;
    border-radius: var(--border-radius-xl);
  }

  @media (max-width: 800px) {
    button {
      background-color: var(--color-on-ui-primary);
      width: 100px;
      justify-self: end;
      border-radius: var(--border-radius-xl);
    }
  }
</style>
